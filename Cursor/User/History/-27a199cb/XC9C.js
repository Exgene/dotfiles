// Select the database to use.
use("mycompany");

// insert person details

db.persons.insertMany([
  // name, age bio, hobbies = [], experience = [{company, duration}], identity ={hasPassport, hasAadhar}

  